This folder is an archived modular prototype.

It is not used by the production extension build/runtime. The active source of truth is root `content.js`.

Files are retained for historical reference and for tests that validate isolated utility logic.
